[phases.setup]
nixPkgs = ["nodejs_20", "python311", "gcc"]

[phases.install]
cmds = [
  "npm ci --omit=dev",
  "python3 -m venv /app/.venv",
  "/app/.venv/bin/pip install pymupdf==1.27.2.2"
]

[start]
cmd = "node server.js"
